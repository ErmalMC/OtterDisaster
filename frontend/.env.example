# Optional: override API base URL used by src/utils/aquaSenseApi.js.
# Default is /api, which works with the Vite proxy configured in vite.config.js.
# VITE_API_BASE_URL=/api

# Optional: backend origin for the Vite /api proxy in local development.
VITE_BACKEND_ORIGIN=http://127.0.0.1:5000

